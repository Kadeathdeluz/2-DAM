<UserControl x:Class="EjercicioTema5.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">

    <Grid>

        <Grid.Resources>

            <Style TargetType="TextBlock">

                <Setter Property="FontSize" Value="20"/>

            </Style>
            
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding Etiqueta}"  Margin="5"/>

        <TextBlock Grid.Column="1" Text="{Binding Contador}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5"/>

        <TextBox Grid.Row="1" Grid.ColumnSpan="2" x:Name="txtTexto" Text="{Binding Texto, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalAlignment="Center"
                 Height="{Binding AlturaCelda}" FontSize="20" 
                 TextChanged="InputBox_TextChanged"
                 TextWrapping="Wrap"/>

    </Grid>
</UserControl>
